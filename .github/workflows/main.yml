name: website06
on: push
jobs:
  building-docker-image:
    runs-on: self-hosted
    steps:
      - name: Checkout code 
        uses: actions/checkout@v3
      - name: Build Image 
        run: docker build -t jaibw/website06 .
      - name: List Image
        run: docker images
